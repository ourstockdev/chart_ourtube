name: chart_ourtube
description: Chart Module for Ourtube.
version: 1.0.0
homepage: https://github.com/ourstockdev/chart_ourtube.git

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
